# 02 — `dev-index`

Owns F4. No move, rename or trash.

## Operations

### OP-4 — Root `index.md`
- Delete the `Dev/wk/` entry (line 106) — the folder is trashed by OP-1.
- Correct the note count on lines 90 and 104: 79 → the count after execution.
- Leave the `Dev/LLM/` entry and the five-group routing line as they are; both stay true.

### OP-5 — `Dev/index.md`
- Re-describe only what changed. After OP-1 the grouped layout is unchanged, so the folder sections
  and the Business Glossary stay as written.
- Add a `Dev/practices/` line for the appended section, if D2 is accepted as recommended.
- No entry is added for any trashed folder.

### OP-6 — `Quick note/` index entry
Only if D1 lands there: add the file line and its one-line description.

### OP-7 — `.rag/bin/rag update --quiet`
Once, after every write. Not `index --full`.
